# RefurbX Fullstack E-commerce

Starter skeleton project for refurbished items e-commerce.


## Frontend UI
Added a React + Tailwind frontend in `frontend/` with Home, Product, Cart, and Login pages. Run `npm install` then `npm run dev` inside `frontend`.
